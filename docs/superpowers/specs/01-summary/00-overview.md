# 执行摘要

---

## 1. 项目概述

本报告定义了一个**层次化、分布式、可扩展的图计算框架**的完整架构。该框架旨在为多种应用场景提供统一的图计算能力，包括但不限于：

- **代码关系分析**：代码库的结构、依赖关系、影响范围
- **业务逻辑编排**：条件分支、循环迭代、并行执行
- **AI Agent 工作流**：多步骤推理、工具调用、人机协作

---

## 2. 核心特性

| 特性 | 设计决策 |
|------|---------|
| **执行模型** | BSP (Bulk Synchronous Parallel) 超步骤模型 |
| **状态管理** | 节点分片 + 消息传递的混合模式 |
| **容错机制** | 增量 Checkpoint + 超步骤边界恢复 |
| **端点设计** | 多 Endpoint 同方向 + 类型安全 |
| **技术栈** | TypeScript/Node.js + Redis + PostgreSQL + Kafka |
| **部署模式** | 多进程/多机容器化部署 (Docker + Kubernetes) |

---

## 3. 设计目标

1. **通用性**: 一个框架支持多种场景（代码关系、逻辑编排、AI工作流）
2. **可扩展性**: 支持横向扩展，适应不同规模
3. **容错性**: 支持断电/故障恢复，Long-running 任务不中断
4. **可观测性**: 提供完整的监控、日志、调试能力
5. **易用性**: 简洁的 API，降低使用门槛

---

## 4. 关键架构特点

### 4.1 多 Endpoint 同方向设计

```
节点可以拥有多个同方向的端点：

输入端点: in:user, in:tool, in:memory, in:control
输出端点: out:llm, out:tool, out:memory, out:error
```

**优势**:
- 清晰地区分不同类型的消息
- 类型安全，编译时检查
- 支持复杂的节点职责划分

### 4.2 混合状态管理

```
子图内：集中式 State（简化编程）
跨子图：消息传递（解耦）
```

### 4.3 增量 Checkpoint

```
每个 superstep 边界保存状态 diff
每 N 个 superstep 保存一次全量 base
支持 replay 恢复
```

---

## 5. 技术架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                         Graph Runtime                               │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │                     Graph Manager                            │    │
│  │  • 图注册/注销           • 节点生命周期管理                   │    │
│  │  • 版本控制              • 快照管理                          │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                              │                                      │
│  ┌───────────────────────────┼───────────────────────────────────┐  │
│  │                    Superstep Executor                         │  │
│  │                                                            │  │
│  │   ┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐   │  │
│  │   │ Node A  │   │ Node B  │   │ Node C  │   │ Node D  │   │  │
│  │   │Endpoints│   │Endpoints│   │Endpoints│   │Endpoints│   │  │
│  │   └────┬────┘   └────┬────┘   └────┬────┘   └────┬────┘   │  │
│  │        │             │             │             │         │  │
│  │        └─────────────┼─────────────┼─────────────┘         │  │
│  │                      ▼                                      │  │
│  │              ┌───────────────┐                              │  │
│  │              │ Message Router│                              │  │
│  │              └───────┬───────┘                              │  │
│  │                      │                                      │  │
│  │   ┌─────────────────┼─────────────────┐                   │  │
│  │   │         Checkpoint Manager          │                   │  │
│  │   └─────────────────┼─────────────────┘                   │  │
│  │                      │                                      │  │
│  └──────────────────────┼──────────────────────────────────────┘  │
│                         │                                         │
└─────────────────────────┼───────────────────────────────────────────┘
                          │
          ┌───────────────┼───────────────┐
          ▼               ▼               ▼
    ┌──────────┐   ┌──────────┐   ┌──────────┐
    │   Redis  │   │  Kafka  │   │ Postgres │
    │ (Cache)  │   │ (Queue) │   │  (Store) │
    └──────────┘   └──────────┘   └──────────┘
```

---

## 6. 决策状态

| 决策类别 | 状态 |
|---------|------|
| 已确认决策 | 10 项 |
| **待确认决策** | **5 项**（Endpoint 命名、连接方式、路由规则等） |

**待确认决策详情**: [04-decisions/00-pending](../04-decisions/00-pending.md)

---

## 7. 文档结构

```
docs/superpowers/specs/
├── README.md                    # 主索引
├── 01-summary/                  # 概述
├── 02-requirements/             # 需求分析
├── 03-concepts/                 # 核心概念（Graph, Node, Endpoint, Message, Superstep）
├── 04-decisions/                # 架构决策（待确认/已确认）
├── 05-architecture/             # 核心架构
├── 06-deployment/               # 分布式部署
├── 07-scenarios/               # 场景扩展
├── 08-fault-tolerance/         # 容错与恢复
├── 09-observability/            # 可观测性
└── 10-impl-plan/               # 实施计划
```

---

## 8. 下一步

1. 确认 [待决策事项](../04-decisions/00-pending.md)
2. 细化已确认的设计
3. 开始 Phase 1 实现
